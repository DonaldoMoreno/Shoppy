# Shoppy API (NestJS)

API skeleton for the Shoppy MVP. Swagger is available at /api/docs when server is running.

Useful commands:
- pnpm dev — run in development mode
- pnpm prisma:generate — generate Prisma client
- pnpm prisma:migrate — run migrations (dev)
- pnpm seed — run seed script

Seeded users:
- admin@shoppy.test / password123 (ADMIN)
- client@shoppy.test / password123 (CLIENT)
- shopper@shoppy.test / password123 (SHOPPER)
