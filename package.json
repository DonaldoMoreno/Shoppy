{
  "name": "shoppy-monorepo",
  "private": true,
  "devDependencies": {},
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -w --parallel dev",
    "install:all": "pnpm install",
    "seed": "pnpm --filter @shoppy/api -- prisma db push --accept-data-loss && pnpm --filter @shoppy/api -- ts-node prisma/seed.ts",
    "lint": "pnpm -w lint"
  }
}
